export type User = {
  id: number;
  name: string;
};

export type mockUser = {
  name: string;
  email: string;
  list: number;
  country: string;
};

export type TodoItem = {
  _id: string;
  description: string;
  isSuccess: boolean;
};

export type UserInfo = {
  name: string;
  birth: string;
};
